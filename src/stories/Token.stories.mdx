import { Meta, Story, ArgsTable, Canvas, Preview } from "@storybook/addon-docs";
import { Provider } from 'react-redux';

import { store } from '@/app/store';

import TokenButton from '@/app/components/TokenButton';
import TokenTree from '@/app/components/TokenTree';

<Meta 
	title="Token"
	component={TokenTree}
/>

<ArgsTable of={TokenTree}/>

export const TokenDefaultStory = (args) => {
	return (
		<Provider store={store}>
			<TokenTree />
		</Provider>
	)
}

<Canvas>
	<Story name="TokenButton" args={{
	}}>
		{TokenDefaultStory.bind({})}
	</Story>
	<Story name="TokenSection" args={{
	}}>
		{TokenDefaultStory.bind({})}
	</Story>
	<Story name="TokenTree" args={{
	}}>
		{TokenDefaultStory.bind({})}
	</Story>
</Canvas>