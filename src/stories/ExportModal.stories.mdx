import { Meta, Story, ArgsTable, Canvas, Preview } from "@storybook/addon-docs";
import { Provider } from 'react-redux';
import { store } from '@/app/store';
import ExportModal from '@/app/components/modals/ExportModal';


<Meta title="ExportModal"/>

<Story name="ExportModal">
	<Provider store={store}>
		<ExportModal />
	</Provider>
</Story>

<ArgsTable of={ExportModal} />

export const TemplateExportModal = (args) => {
	return (
		<ExportModal {...args} />
	)
}
