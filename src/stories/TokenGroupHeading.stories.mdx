import { Meta, Story, ArgsTable, Canvas, Preview } from "@storybook/addon-docs";
import TokenGroupHeading from '@/app/components/TokenGroupHeading';
import { Provider } from 'react-redux';
import { store } from '@/app/store';

<Meta title="TokenGroupHeading"/>

<ArgsTable of={TokenGroupHeading} />

<Story name="Standrad: TokenGroupHeading">
  <Provider store={store}>
    <TokenGroupHeading label="standarad" path="standard" id="listing" />
  </Provider>
</Story>

export const TemplateTokenGroupHeading = (args) => {
  return (
    <Provider store={store}>
      <TokenGroupHeading {...args} />
  </Provider>
  )
}

<Canvas>
  <Story name="Primary" args={{
    label: "Primary",
    path: "default.token",
    id: "listing",
  }}>
    {TemplateTokenGroupHeading.bind({})}
  </Story>
</Canvas>