import { useState } from 'react';
import { Meta, Story, ArgsTable, Canvas, Preview } from "@storybook/addon-docs";
import Button from '@/app/components/Button/Button';
import ConfirmLocalStorageModal from "@/app/components/modals/ConfirmLocalStorageModal";

<Meta title="ConfirmLocalStorageModal" />

export const ConfirmLocalStorageModalStoryPreview = () => {
  const [isOpen, setIsOpen] = useState(false);
  return (
    <>
      <Button onClick={() => setIsOpen(true)}>
        Open a Confirmation Modal
      </Button>
      <ConfirmLocalStorageModal 
        isOpen={isOpen} 
        onClose={() => setIsOpen(false)} 
      />
    </>
  );
};

<Story name="Default">
  <ConfirmLocalStorageModalStoryPreview />
</Story>

<ArgsTable of={ConfirmLocalStorageModal}/>

export const TemplateConfirmLocalStorageModal = (args) => {
  return (
    <ConfirmLocalStorageModal isOpen={true}/>
  )
}

<Canvas>
  <Story name="Buttons aligned right" args={{
    isOpen: true,
  }}>
    {TemplateConfirmLocalStorageModal.bind({})}
  </Story>
</Canvas>