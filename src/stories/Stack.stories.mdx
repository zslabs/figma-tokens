import { Meta, Story, ArgsTable, Canvas, Preview } from "@storybook/addon-docs/blocks";
import Stack from '@/app/components/Stack';

<Meta title="Stack"/>

<Story name="Standrad: Stack">
  <Stack gap={0} direction={'row'} align={'center'} justify={'center'} width={'full'}></Stack>
</Story>

<ArgsTable of={Stack} />

export const TemplateStack = (args) => {
  return (
    <Stack {...args}>33333333333333ffffffffffffffff</Stack>
  )
}

<Canvas>
  <Story name="Primary" args={{
    gap: 1,
    direction: 'column',
    align: 'start',
    justify: 'start',
    wrap: false,
  }}>
  {TemplateStack.bind({})}
  </Story>
</Canvas>